# ⚡ Быстрый старт - 5 минут

## Предварительные требования
- ✅ Docker Desktop установлен и запущен
- ✅ Есть Telegram бот токен
- ✅ Есть API ключ для AI (Timeweb или YandexGPT)

## 1. Настройка (2 минуты)

```bash
# Скопировать пример конфигурации
cp .env.example .env

# Открыть .env и заполнить:
# - POSTGRES_PASSWORD (любой надежный пароль)
# - SECRET_KEY (любая случайная строка 32+ символов)
# - TELEGRAM_TOKEN (от @BotFather)
# - TIMEWEB_API_KEY (или YandexGPT ключи)
```

**Минимальная конфигурация .env:**
```env
POSTGRES_PASSWORD=MySecurePass123!
SECRET_KEY=random_string_at_least_32_characters_long
TELEGRAM_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TIMEWEB_API_KEY=your_timeweb_api_key
```

## 2. Запуск (2 минуты)

```bash
# Запустить все сервисы
docker compose up -d

# Дождаться запуска (30-60 сек)
docker compose ps

# Применить миграции БД
docker compose exec api alembic upgrade head
```

## 3. Проверка (1 минута)

### Web UI
Откройте: http://localhost:8501

Должна открыться главная страница Банкрот ПРО

### API
Откройте: http://localhost:8000/docs

Должна открыться Swagger документация

### Telegram Bot
1. Откройте Telegram
2. Найдите ваш бот по username
3. Отправьте `/start`
4. Должно прийти приветствие с дисклеймером

## 4. Первое дело (2 минуты)

### Через Telegram бот:
```
/новое_дело
Иванов Иван Иванович
500000
Сбербанк
300000
Нет
```

### Через Web UI:
1. Откройте http://localhost:8501
2. Перейдите в "➕ Новое дело"
3. Заполните форму
4. Нажмите "Создать дело"

## Готово! 🎉

Теперь вы можете:
- ✅ Создавать дела через бот и веб
- ✅ Просматривать список дел
- ✅ Задавать вопросы AI-ассистенту
- ✅ Генерировать документы (после создания шаблона)

## Полезные команды

```bash
# Просмотр логов
docker compose logs -f

# Остановить
docker compose stop

# Запустить снова
docker compose start

# Перезапустить
docker compose restart

# Удалить всё
docker compose down -v
```

## Если что-то не работает

### Бот не отвечает
```bash
# Проверить логи бота
docker compose logs bot

# Перезапустить бота
docker compose restart bot
```

### Web не открывается
```bash
# Проверить логи
docker compose logs web

# Проверить, что API работает
curl http://localhost:8000/health
```

### База данных не работает
```bash
# Проверить PostgreSQL
docker compose logs postgres

# Пересоздать с миграциями
docker compose down
docker compose up -d
docker compose exec api alembic upgrade head
```

## Что дальше?

1. **Создайте шаблон документа** - см. [api/templates/README_TEMPLATE.md](api/templates/README_TEMPLATE.md)
2. **Настройте production** - см. [SETUP.md](SETUP.md)
3. **Изучите API** - http://localhost:8000/docs
4. **Запустите тесты** - `docker compose exec api pytest`

---

**Нужна помощь?** Читайте [README.md](README.md) и [SETUP.md](SETUP.md)

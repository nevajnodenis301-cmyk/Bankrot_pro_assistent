# ⚖️ Банкрот ПРО

**Версия:** 0.9.0 - Production Deployed

**Production:** https://bankrot.denis-lab.ru

Комплексная система управления делами по банкротству физических лиц для юристов. Реализует полный цикл работы в соответствии с 127-ФЗ РФ "О несостоятельности (банкротстве)".

## Статус проекта

| Компонент | Статус |
|-----------|--------|
| Deployment | Production (https://bankrot.denis-lab.ru) |
| Security | HTTPS/SSL enabled (Let's Encrypt) |
| Backups | Automated daily at 2 AM |
| Authentication | JWT + AES-256-GCM encryption |
| Firewall | Configured (ports 22, 80, 443 only) |

## 🚀 Возможности

### 🖥️ Web-интерфейс (Streamlit)
- Создание и редактирование дел с полными данными
- Хранение конфиденциальной информации (паспорт, ИНН, СНИЛС)
- Управление кредиторами
- Генерация документов (заявления о банкротстве)
- Статистика и аналитика по делам

### 🤖 Telegram Bot
- Быстрое создание дел в мобильном формате
- Просмотр списка дел (только публичные данные)
- AI-ассистент по вопросам 127-ФЗ
- Юридический дисклеймер для клиентов

### 🔒 Безопасность
- **Конфиденциальные данные** (паспорт, ИНН) доступны только через защищенный веб-интерфейс
- **Telegram-бот** работает только с публичными данными (ФИО, номер дела, сумма долга)
- Разделение уровней доступа между каналами

### 🤖 AI-ассистент
- Мульти-провайдер поддержка: Timeweb Cloud AI, YandexGPT
- Ответы на вопросы по 127-ФЗ
- Помощь в понимании процедур банкротства

## 🏗️ Архитектура

```
bankrot_pro/
├── api/                    # FastAPI backend
│   ├── models/            # SQLAlchemy модели
│   ├── schemas/           # Pydantic схемы
│   ├── routers/           # API endpoints
│   ├── services/          # Бизнес-логика
│   └── templates/         # Шаблоны документов
├── bot/                   # Telegram бот (aiogram)
│   ├── handlers/          # Обработчики команд
│   ├── states/            # FSM состояния
│   ├── keyboards/         # Клавиатуры
│   └── services/          # API клиент
├── web/                   # Streamlit веб-интерфейс
│   └── pages/             # Страницы приложения
├── alembic/               # Миграции БД
└── tests/                 # Тесты
```

## 📦 Стек технологий

- **Backend**: FastAPI, SQLAlchemy, Alembic
- **Database**: PostgreSQL, Redis
- **Bot**: aiogram 3.x
- **Web UI**: Streamlit
- **Documents**: python-docx, docxtpl
- **AI**: Timeweb Cloud AI / YandexGPT (настраиваемо)
- **Infrastructure**: Docker, Docker Compose

## 🚀 Быстрый старт

### 1. Клонирование и настройка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd bankrot_pro

# Создать .env файл
cp .env.example .env
```

### 2. Настройка переменных окружения

Отредактируйте `.env`:

```env
# PostgreSQL
POSTGRES_USER=bankrot
POSTGRES_PASSWORD=your_secure_password

# Security
SECRET_KEY=your_secret_key_32_characters

# Telegram Bot
TELEGRAM_TOKEN=your_bot_token_from_botfather

# AI Provider (timeweb или yandexgpt)
AI_PROVIDER=timeweb
TIMEWEB_API_KEY=your_timeweb_api_key
```

### 3. Запуск с Docker Compose

```bash
# Запустить все сервисы
docker compose up -d

# Просмотреть логи
docker compose logs -f

# Остановить
docker compose down
```

### 4. Инициализация базы данных

```bash
# Войти в контейнер API
docker compose exec api bash

# Выполнить миграции
alembic upgrade head
```

## 🌐 Доступ к сервисам

После запуска доступны:

- **API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs
- **Web UI**: http://localhost:8501
- **Telegram Bot**: @your_bot_username

## 📖 Использование

### Web-интерфейс

1. Откройте http://localhost:8501
2. Перейдите в "➕ Новое дело"
3. Заполните все данные клиента
4. Добавьте кредиторов
5. Сохраните дело
6. Скачайте сгенерированные документы

### Telegram Bot

```
/start - начало работы
/новое_дело - создать дело (упрощенный режим)
/список_дел - просмотр ваших дел
/ai [вопрос] - спросить AI-ассистента
/помощь - справка
```

### API

```bash
# Создать дело
curl -X POST http://localhost:8000/api/cases \
  -H "Content-Type: application/json" \
  -d '{"full_name": "Иванов Иван Иванович", "total_debt": 500000}'

# Получить список дел
curl http://localhost:8000/api/cases

# Получить полную информацию по делу
curl http://localhost:8000/api/cases/1

# Получить публичную информацию (для бота)
curl http://localhost:8000/api/cases/1/public

# Скачать документ
curl http://localhost:8000/api/documents/1/bankruptcy-application -o bankruptcy.docx
```

## 🧪 Тестирование

```bash
# Установить зависимости для тестов
pip install pytest pytest-asyncio httpx

# Запустить тесты
pytest tests/ -v
```

## 📄 Генерация документов

### Создание шаблона заявления

1. Создайте документ Word с плейсхолдерами:
   - `{{ case_number }}` - номер дела
   - `{{ full_name }}` - ФИО должника
   - `{{ passport }}` - паспортные данные
   - `{{ total_debt }}` - сумма долга
   - `{% for creditor in creditors %}...{% endfor %}` - список кредиторов

2. Сохраните как `api/templates/bankruptcy_application.docx`

Подробнее см. [api/templates/README_TEMPLATE.md](api/templates/README_TEMPLATE.md)

## 🔧 Разработка

### Локальный запуск без Docker

#### API

```bash
cd api
pip install -r requirements.txt

# Настроить DATABASE_URL в .env
export DATABASE_URL="postgresql+asyncpg://bankrot:password@localhost:5432/bankrot"

# Выполнить миграции
alembic upgrade head

# Запустить
uvicorn main:app --reload
```

#### Bot

```bash
cd bot
pip install -r requirements.txt

# Настроить переменные
export TELEGRAM_TOKEN="your_token"
export API_BASE_URL="http://localhost:8000"

# Запустить
python main.py
```

#### Web

```bash
cd web
pip install -r requirements.txt

# Настроить
export API_BASE_URL="http://localhost:8000"

# Запустить
streamlit run app.py
```

### Создание новой миграции

```bash
# Войти в контейнер
docker compose exec api bash

# Создать миграцию
alembic revision --autogenerate -m "description"

# Применить
alembic upgrade head
```

## 🔐 Безопасность и конфиденциальность

### Уровни доступа

1. **Telegram Bot** (публичные данные):
   - Номер дела
   - ФИО должника
   - Сумма долга
   - Статус дела
   - Количество кредиторов

2. **Web/API** (полные данные):
   - Все публичные данные +
   - Паспортные данные
   - ИНН, СНИЛС
   - Адрес регистрации
   - Контактные данные
   - Финансовая информация

### Рекомендации

- Используйте сильные пароли для БД
- Регулярно обновляйте `SECRET_KEY`
- Настройте HTTPS для production
- Ограничьте доступ к API через firewall
- Регулярно делайте бэкапы БД

## 📊 Статусы дел

- `new` - Новое дело
- `in_progress` - В работе
- `court` - Передано в суд
- `completed` - Завершено

## 🤝 AI-провайдеры

### Timeweb Cloud AI (по умолчанию)

```env
AI_PROVIDER=timeweb
TIMEWEB_API_KEY=your_key
TIMEWEB_API_URL=https://api.timeweb.cloud/v1
```

### YandexGPT

```env
AI_PROVIDER=yandexgpt
YANDEXGPT_API_KEY=your_key
YANDEXGPT_FOLDER_ID=your_folder_id
```

## ⚠️ Юридический дисклеймер

Данная система предоставляет информационную поддержку и **НЕ является юридической консультацией**.

Для принятия решений по конкретным делам всегда обращайтесь к квалифицированному юристу.

AI-ассистент предоставляет справочную информацию по 127-ФЗ и не может заменить профессионального юридического анализа.

## 📝 Лицензия

MIT License

## 🤝 Поддержка

По вопросам работы системы:
- Создайте Issue в репозитории
- Напишите на support@example.com

---

**Версия**: 0.9.0 (Production Deployed)
**Дата обновления**: January 31, 2026
**Roadmap**: [ROADMAP.md](ROADMAP.md)
